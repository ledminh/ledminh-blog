import { categories } from "../../../../../../data";

const initialState = {
    catsList: categories 
};



const dataReducer = (state = initialState, action) => {
   

    return state;
};




export default dataReducer;